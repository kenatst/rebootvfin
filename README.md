# REBOOT iOS

Native SwiftUI application for REBOOT (iOS 17+).

This repository contains the REBOOT app, Xcode project, Screen Time extensions, bundled resources, and native assets.

Open `REBOOT.xcodeproj` in Xcode and use the `REBOOT` scheme.
